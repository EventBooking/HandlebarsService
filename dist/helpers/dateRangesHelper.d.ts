import "twix";
export declare const IsoDate = "YYYY-MM-DD";
export declare function isContiguosDate(prev: string, curr: string): boolean;
export declare function dateRangesHelper(dates: string[], separator: string, pattern: string, locale: string): string;
